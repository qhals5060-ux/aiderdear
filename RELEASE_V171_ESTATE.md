# v171 — ESTATE 사이트 전용 업무 공간

상태: v171 사이트 전용 배포 산출물과 구현·격리 검증 결과. 실제 Firebase 로그인 상태의 종단간 저장 및 Google 전송 검증은 아래 미확인 항목으로 구분한다.

## 1. 적용 범위와 기존 기능 보존

- 사이트 상단 PERSONAL 오른쪽에 ESTATE 탭을 추가했다. 내부 메뉴는 오늘의 업무 / 매물 관리 / 고객 관리 / 매물 매칭 / 거래 진행 / 정산·통계다.
- 기존 vanilla HTML/JS, Firebase 인증, Vercel API 구성을 사용한다. 프레임워크를 교체하지 않는다.
- ESTATE 모듈과 `.estate-*` 스타일을 분리했다. 앱에서는 ESTATE 모듈을 설치하지 않는다.
- Google OAuth 설정과 기존 `api/calendar-sync.mjs`, `firebase-app.js`, `server/firebase-admin.mjs` 코드를 변경하지 않았다.
- 기존 개인·커플·친구·대학원 컨설팅·Work 저장 데이터를 이전하거나 삭제하지 않는다. 부동산 고객은 Consult 고객과 별도 업무 자료다.
- 이번 기능은 로그인 계정 한 명이 자기 자료를 관리하는 구조다. 담당자 배정·공동 편집·팀 관리 기능을 구현한 것으로 표시하지 않는다. 별도 Work 직원 계정은 ESTATE 접근 대상이 아니다.

## 2. 업무 기능

### 오늘의 업무

- 방문, 고객 연락, 계약·잔금, 매물 확인, 미수 보수를 작은 요약으로 표시한다.
- 기한을 넘긴 업무와 예정 업무를 구분하고 관련 고객·매물·거래 상세를 연다. 업무 완료 및 날짜 변경을 저장할 수 있다.
- 상담의 후속 행동과 방문 후속 연락은 기한이 있을 때 연결 업무를 생성한다. 같은 원본을 수정하면 같은 업무 식별자를 갱신한다.
- 빈 자료를 예시 고객·가짜 통계로 채우지 않는다.

### 매물·고객·거래

- 매물 목록/사진 카드 전환, 조건 필터, 등록·수정, 자동 매물번호, 주소 중복 가능성 안내를 제공한다. 중복 주소를 자동 병합하지 않는다.
- 매매·전세·월세 금액, 면적, 시설, 입주 조건, 확인일, 소유 고객, 내부/공개 설명, 상가·토지 추가 항목을 저장한다. 미확인 값과 확인된 0원을 구분한다.
- 가격·상태 변경 이력을 보존한다. 중개 종료와 거래 완료는 삭제가 아니다.
- 고객의 복수 역할·희망 조건·필수/협의 조건, 상담·제안·방문·거래를 식별자로 연결한다.
- 거래는 보드/목록으로 보고 단계를 변경한다. 보류·중단 사유, 계약·중도금·잔금·인도 날짜, 체크리스트, 첨부 자료를 저장한다. 체크리스트 완료를 법적 검토 완료로 표현하지 않는다.
- 중개보수는 예상액·확정액을 직접 입력한다. 법정 요율을 자동 계산하는 기능은 없다. 분할 수납은 별도 수납 기록으로 누적하며 예상 수익과 실제 수납을 구분한다.
- 공통 검색과 관련 자료 선택은 나누어 조회한다. 더 조회할 자료가 남으면 이어서 찾을 수 있으며, 현재 조회 범위를 전체 자료 검색 완료처럼 표시하지 않는다.

### 매칭·비교

- 고객 → 매물, 매물 → 고객 방향으로 가격·지역·유형·면적·입주일·시설 조건을 비교한다.
- 충족 / 불충족 / 협의 필요 / 정보 미확인을 이유와 함께 표시한다. 필수 조건이 확인되지 않거나 충족되지 않은 후보는 기본 추천에서 제외한다.
- 적합도와 확인된 정보 비율을 구분한다. 적합도는 계약 성사 확률이 아니다.
- 매물 2~5개를 선택해 공개 범위를 검토하고 읽기 전용 비교 링크를 만든다.

## 3. 저장·권한·공개 링크

- 모든 비공개 자료의 기준 경로는 `estateWorkspaces/{검증된 Firebase UID}`다. 요청 본문의 UID·소유자 필드로 다른 경로를 선택할 수 없다.
- 매물, 고객, 상담, 방문, 거래, 업무, 수납, 제안, 공유 링크, 방문 요청을 별도 컬렉션의 식별자로 연결한다.
- 서버는 Firebase ID 토큰 검증, 허용 필드·자료형·날짜 검사, 소유자 내부 참조 검사를 수행한다. 잘못된 필드나 다른 계정의 참조를 거부한다.
- 수정은 revision 충돌 검사와 requestId 기반 중복 제출 방지를 사용한다. 실패 시 입력창을 유지한다.
- 브라우저는 ESTATE 원본을 Firestore로 직접 읽거나 쓰지 않는다. 서버 Admin SDK 경로를 사용하며, 현재 규칙의 미일치 경로 거부가 새 비공개 루트에 적용되는 것을 코드로 확인했다. 운영 프로젝트에 게시된 규칙과 실제 계정 간 접근은 별도 검증 항목이다.
- 공개 링크는 32바이트 무작위 토큰을 사용하고 서버 인덱스에는 해시를 사용한다. 최대 30일 만료와 소유자의 비활성화를 지원한다.
- 공개 자료는 생성 시 서버 허용 목록으로 구성한 스냅샷이다. 내부 메모, 소유자 연락처, 고객 정보, 공동중개 내부 정보, 가격 변경 이력을 자동 포함하지 않는다.
- 주소와 상세 주소·동호수는 각각 명시적으로 선택해야 공개한다. 사진도 선택한 ID만 공개하며 원본 파일명은 제외한다. 공개 사진 파일명은 일반적인 이름으로 내보낸다.
- 선택된 이미지 자체에 개인정보가 찍혀 있는지는 자동 판독하지 않는다. 공유 전 사진과 공개 설명의 내용을 사용자가 확인해야 한다.
- 공개 방문 희망은 동의 후 요청 상태로만 접수한다. 중개사가 기존 고객·매물을 선택해 승인해야 방문 일정이 생긴다. 같은 요청의 반복 승인은 방문을 중복 생성하지 않는다.
- 만료·비활성 링크는 자료/사진 조회와 요청 제출을 거부한다. 공개 요청은 크기·횟수 제한을 적용한다.

## 4. 사진·문서와 정리

- 사진 업로드 전 압축과 목록용 작은 이미지를 사용한다. 사진 대표 순서, 문서 첨부, 개별 다운로드·제거를 지원한다.
- 다운로드 파일 준비 후 직접 누를 수 있는 `파일 저장 · 파일명` 링크를 유지한다. 자동 다운로드를 저장 완료로 간주하지 않는다. 화면 종료·계정 변경·첨부 제거 시 준비된 파일 URL을 해제한다.
- 파일 한 개의 상한은 12 MiB다. 384 KiB 단위로 나누어 저장하고, 완료 시 전체 해시·청크 구성·파일 형식을 확인한다. 지원 형식만 받으며 확장자만으로 신뢰하지 않는다.
- 파일 바이트는 분할 저장되므로 무료 사용량을 소비하지 않는 구조가 아니다. Firestore 저장 용량 및 읽기/쓰기 사용량을 소비한다. 새 유료 상품이나 스토리지를 개통하지 않았다.
- 유효한 매물·거래·공개 링크에서 참조하는 파일의 삭제를 막는다. 교체/제거 시 원본 참조를 먼저 수정한 뒤 미참조 파일을 정리한다.
- 정리 대상은 오래된 미완료 업로드(24시간)와 미참조 완료 파일(7일)이다. 고객·매물·계약 기록을 대상으로 하는 삭제가 아니다.
- 파일 삭제는 먼저 업로드를 잠그고 최대 8청크씩 작은 트랜잭션으로 처리한다. 중간 실패 뒤 동일 작업 ID로 재시도해 이어서 삭제할 수 있다. 최대 크기 파일의 최종 검증은 큰 바이트 전체를 쓰기 트랜잭션 안에서 읽지 않는다.
- 원본 업무 자료는 90일이 지났다는 이유로 삭제하지 않는다. 이번 변경은 기존 자료의 일괄 압축/이전 작업도 실행하지 않는다.
- 향후 사용량 고려: 저장 중복 방지 영수증은 결과 스냅샷을 보관하므로 수정 횟수에 따라 용량이 증가한다. fingerprint와 재시도 의미를 유지하는 무손실 압축 방안을 별도로 검토할 수 있다. 이 릴리스에서 영수증을 임의 삭제하거나 운영 자료를 변환하지 않았다.

## 5. 기존 캘린더 연결

- 방문·계약 관련 날짜와 후속 업무를 `부동산 업무` 분류로 읽기 전용 투영한다. 식별자는 원본 종류·원본 ID·날짜 항목으로 고정한다.
- 원본 날짜 수정은 같은 투영을 갱신한다. 개인 일정 저장 배열, 커플 일정, 친구 공유 payload에 ESTATE 원본을 복제하지 않는다.
- 캘린더에서 부동산 일정을 선택하면 관련 ESTATE 원본 상세로 이동한다. 날짜 변경은 원본에서 한다.
- 소유자 변경/로그아웃 시 캐시를 비우고 이전 계정의 지연 응답을 폐기한다. 무관한 페이지와 숨겨진 화면에서는 추가 일정 조회를 시작하지 않는다.
- Google 전송은 사이트 저장과 별도의 수동 동작이다. 정상 연결된 선택 대상 중 본인 소유 캘린더만 UI에서 고르고, 외부 제목·설명을 검토해야 전송한다. 기본 제목은 `부동산 업무`, 설명은 공란이며 고객 연락처와 내부 메모를 자동 채우지 않는다.
- 전송은 저장된 최신 날짜를 다시 확인한다. 검토 후 날짜가 변경되면 재검토를 요구한다. Google 실패가 사이트 저장을 취소하지 않는다.
- **Google 제한:** 기존 Google `create → sync` 경로를 재사용하므로 전송한 별도 사본이 사이트의 Google 일정으로 재수입될 수 있다. 이 경우 ESTATE 원본 투영과 Google 사본이 함께 보일 수 있다. 원본과 Google 사본의 자동 갱신·중복 병합은 구현하지 않았다. 새로 전송하면 사본이 추가될 수 있으며 UI에 이를 안내한다. 한 전송 양식의 성공 후 재전송은 막지만 새 검토까지 포함한 전역 중복 전송 방지는 아니다.

## 6. 정산·통계와 외부 정보의 한계

- 계약일, 수납일, 수납 예정일 등 지표별 기준을 표시한다. 분할 수납의 누적액과 기간 내 실제 수납을 구분한다.
- 기간 필터, 계약 건수, 실제 수납, 미수금, 예상 보수, 거래 유형/문의 경로/문의→방문→계약 전환, 계약까지 걸린 기간을 제공한다.
- 조회한 자료 범위를 표시한다. 페이지를 더 불러오지 않았다면 전체 자료 집계로 간주하지 않는다. 표본이 없으면 데이터 없음/계산 불가로 표시한다.
- 보류·중단 등 제외 기준과 계산 기준을 화면에서 확인할 수 있다.
- 외부 서비스는 공식 확인용 바로가기와 수동 입력을 제공한다. 실거래가·시세 API에 실제로 연결된 것처럼 표시하지 않는다. 자동 데이터 수집·법정 중개보수 계산은 이번 구현 범위가 아니다.

## 7. 격리 자동 검증

검증 명령은 저장소 루트에서 Node.js 24로 실행했다. 실제 인증 키·운영 Firebase·Google API에 쓰지 않는 메모리 DB/DOM 대역을 사용한다. 실제 서버 dispatch, 모델 검증기, 화면 모듈을 실행하지만 운영 인프라와 브라우저 레이아웃 전체를 대체하지 않는다.

| 명령 | 결과 | 범위 |
| --- | --- | --- |
| `node --test tests/estate-api-v171.mjs tests/estate-domain-v171.mjs` | 27/27 통과 | API 16 + 도메인/클라이언트 11: 권한, 검증, 저장·재시도, 공유·파일·매칭·통계 |
| `node --test tests/test-estate-workflow-v171.mjs tests/test-estate-integration-v171.mjs` | 58/58 통과 | 업무/공개 화면 10 + 컨트롤러/캘린더/다운로드 통합 48 |
| `node tests/test-estate-directory-v171.mjs` | 내부 검증 49/49 통과 | 실제 매물/고객 모듈 + 실제 필드 도우미 + 실제 서버 모델 검사 |
| `node --test tests/site-typography-v169.test.cjs` | 내부 검증 9/9 통과 | 기존 사이트 폰트·테마 범위 보존 |

위 네 파일 그룹을 한 번에 `node --test`로 실행하면 테스트 러너는 87개의 통과 항목으로 집계한다. 디렉터리·폰트 검증 파일은 각각 러너 항목 1개 안에 내부 검사 49개·9개가 있으므로 동일 단위의 테스트 수처럼 중복 합산하지 않는다.

주요 보안/실패 회귀 범위:

- 일반 계정 소유권, 다른 UID 참조 거부, Work 직원 제외, 경로 조작, 알 수 없는 필드, 만료·취소 토큰.
- 동일 요청 재시도와 내용이 달라진 요청 충돌, revision 충돌, 저장 실패 시 입력 유지 및 이중 제출 차단.
- 공유 자료 허용 목록, 원본 파일명 배제, 사진 선택 제한, 링크 만료/비활성, 동의/횟수 제한, 승인 전 방문 미생성.
- 최대 12 MiB 파일 완성, 8청크 삭제 중단·재시도, 활성 참조 삭제 거부, 다른 업로드 nonce로 재생성된 파일에 오래된 완성 요청 거부.
- 필수 조건 미확인 제외, 0원 관리비, 분할 수납과 기간 기준, 비어 있는 통계, 계정 변경 중 응답·파일 URL 폐기.
- 캘린더 고정 ID, 중복 후속 업무 제거, 저장 도중 갱신 무효화, 숨겨진 화면 조회 중단, 이전 소유자 데이터 폐기.

## 8. 격리 브라우저 검증

실제 사이트 자산 + 실제 ESTATE API dispatch + 서버 메모리 저장소를 사용하는 로컬 fixture에서 다음 흐름을 확인했다. 자료는 테스트용이며 운영 DB에 등록하지 않았다. 새로고침 유지 검증은 fixture 서버가 살아 있는 동안의 지속성이다.

- 매물 2개와 고객 1개를 실제 양식으로 등록하고 새로고침 후 다시 조회.
- 필수 예산 조건 불일치 후보를 기본 추천에서 제외.
- 공개 범위 미리보기 → 비교 링크 → 공개 방문 요청 → 중개사 승인 → 방문 생성.
- 방문 날짜를 9일에서 10일로 수정 → 기존 사이트 캘린더에서 새 날짜 한 건 확인 → 일정을 눌러 원본 상세 열기.
- 계약의 확정 보수 100만원에 40만원·20만원 분할 수납 → 실제 수납 60만원 / 미수 40만원 확인.
- 고객 상담에서 다음 행동과 기한 저장 → 오늘의 업무 목록에 반영.
- 테스트 사진의 압축·썸네일 생성과 문서 첨부 저장 → 새로고침 뒤 다시 조회.
- 다른 fixture 계정으로 전환하면 앞 계정의 매물이 노출되지 않음 → 원래 계정으로 돌아오면 자료 유지.
- 모던·에디토리얼 탭 진입과 목록 전환, 1440px 데스크톱·1024px 작은 노트북·390px 모바일 사이트 배치를 확인. 모바일 상세 패널의 닫기와 저장 버튼이 화면 안에 있고 상단 헤더와 겹치지 않도록 수정.

브라우저에서 준비된 문서 Blob 링크와 직접 저장 버튼은 확인했지만, 인앱 브라우저의 다운로드 완료 이벤트는 자동·직접 클릭 모두 시간 초과했다. 실제 파일이 사용자의 다운로드 폴더까지 저장됐다고 주장하지 않는다. 실제 Chrome과 휴대폰에서의 최종 파일 저장 확인은 남아 있다. 화면 크기 검사는 브라우저 뷰포트 검사이며 실물 휴대폰 검사와 다르다.

## 9. 운영 전 추가 확인

- 실제 배포 환경의 Firebase Admin 자격 증명과 현재 게시된 Firestore 규칙에서 인증 사용자 저장/재로그인 유지, 다른 실제 계정 접근 거부.
- 운영 공개 링크의 만료·비활성·선택 사진 접근과 실제 고객 요청 처리.
- 실파일의 사진 압축/업로드/다운로드/교체/정리 및 네트워크 단절 후 재시도.
- 실제 Google 연결·선택 캘린더·전송 결과 및 별도 사본 재수입 동작. OAuth 설정은 변경하지 않는다.
- 실제 휴대폰/작은 노트북에서 터치·파일 선택·상세 패널·저장 버튼 접근성.
- 운영 저장량과 읽기/쓰기 사용량. 격리 테스트 통과를 무료 할당량 내 운영 보장으로 표현하지 않는다.

## 10. 배포·패키지·복구 정보

- 운영 대상: `https://aiderdear1.vercel.app/?release=171&site-edition=modern`.
- 기존 Git → Vercel production 연결을 사용한다. 인증 설정 변경이나 새 유료 서비스 추가 없이 ESTATE API 1개를 추가한다.
- PC 배포: `AiderLog-Modern-v171-site-files.zip`, `AiderLog-Editorial-v171-site-files.zip`. 실제 브라우저 소스 바이트·필수 파일·다운로드 링크·서버 비밀정보 제외를 패키징 검사한다. ZIP은 독립 실행형 프로그램이 아니라 HTTPS 사이트 시작 링크와 브라우저 소스 묶음이다.
- Android는 `AiderLog-v169.apk` 그대로다. SHA-256: `6484fbadc1950110120063c6479d06bad3ce014fa95fc1cc11b624af5ec18b9f`.
- 이전 배포 복구 기준: Git `415f847428bc2bd8b2a251d21cd8c415a82e39b6` / Vercel `dpl_4iMrRpATiJciwPx16Ed6EDHSRo18`. 기존 업무 자료를 이전·삭제하는 DB 마이그레이션은 없다.
- 최종 배포 READY 여부와 HTTP 검사는 별도 작업 결과에서 보고한다. 이 문서는 운영 인증 환경의 실제 저장·전송까지 전부 검증 완료했다는 뜻이 아니다.
