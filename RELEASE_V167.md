# v167 — published 2026-09-07

운영 배포 및 무료 요금제 유지 결과는 `ROLLOUT_V167_RESULT.md`를 확인하세요. 아래 구현 범위에 v13 위젯과 별도 앱 Consult/Work 상세 리디자인은 포함되지 않습니다.

## 반영 내용

- 중단된 앱 My/Paper/어학/Brain/Speech/Study 화면 정리, 글자 크기 .72/.84/1.0 설정을 이어서 반영했습니다. 이전 보통 크기가 새 크게, 이전 작게가 새 보통입니다.
- 최신 시스템 테마 색상 명세로 버튼·입력·카드·텍스트·휠·로고를 연결했습니다. 색상 변경 시 휠의 위치/터치/전환과 레이아웃·폰트 크기는 바꾸지 않습니다. 이전 v166 Soft Purple 기본값 설명보다 `android-src/APP_COLOUR_V167.md`가 우선합니다.
- 앱 시작의 자체 동의 선택을 없앴습니다. 실제 녹음/카메라 사용 시 Android 권한 처리는 유지합니다.
- 사이트 Schedule의 작은 실행 버튼을 년·월 옆으로 이동하고 Calendar/This Month 면적을 늘렸습니다.
- 사이트 Routine은 기존 카드 크기 안에서 글자·스탬프를 줄이고 현재 회차 통계를 함께 표시합니다. 별도 수행기록 박스를 제거하고 전체 통계는 생성 버튼 위에서 엽니다.
- 사이트 Event는 Record/Archive/Travel 선택창, Record와 Album의 약 4:1 화면, 폴더형 앨범, 분류 옆 코드 없는 색상 원, 실제 기록 찾아보기로 정리했습니다.
- 사이트 Consult는 기존 ID·데이터를 보존하는 고객/상담/과업/지원전략/문서 관리와 원본 분리 전달본을 제공합니다.
- Work는 대표 업무/직원/프로젝트/지출·구매·증빙/제출 기록과 독립된 직원 포털로 분리했습니다. 직원 개인 메모·초안은 대표에게 공개되지 않으며 제출본과 검토/보완 버전을 구분합니다.
- 사이트의 세부 화면은 버튼으로 전환하며 선택 뒤 글자 크기가 변하지 않게 했습니다. Paper의 홈은 아이콘으로 옮기고 중복 Home과 요청된 장식성 안내문을 제거했습니다.
- 계정 변경·동시 저장·불명확한 재전송·기존 기록 이전·첨부 원본 잠금·Service Worker 경로별 캐시를 검증했습니다.

## 확인한 결과

- 사이트 Schedule/Routine/Event: 1440/1366/1024/390 화면, 저장·선택·100회 전환과 회차 통계 검증.
- 앱 색상 14개 조합: 측정한 기하·폰트·투명도·전환 값 변화 0, 페이지 오류 0. 별도 라이브 테마 전환·저장·재실행 검사.
- 실제 Firestore API/Rules 114개 권한/저장/첨부 검사; 앱/사이트 Consult 동기화 24개, 위젯과 Consult 동시 저장 16개.
- Consult 모델 15개, 동기화 순수 단위검사 7개, 기존 위젯 트랜잭션 24개.
- 대표/직원 UI는 별도 브라우저 테스트와 실제 에뮬레이터 API 흐름으로 검증했습니다. 구체적 최종 수치는 함께 제공하는 QA JSON을 기준으로 합니다.

## 운영 적용과 남은 검증

후속 운영 전환 요청에 따라 Git push, Vercel 운영 배포, v167 APK/모던·에디토리얼 PC ZIP 생성과 다운로드 교체, 최종 Firebase 규칙 게시를 완료했습니다. 기존 v165 다운로드 주소는 v167로 연결합니다. 실제 친구 초대·기록 이전과 운영 테스트 데이터 입력은 하지 않았습니다.

운영 서버 설정, 실제 로그인 세션 복원 및 인증된 API 조회를 확인했습니다. 신규 Google OAuth 동의·다른 친구 계정·실사용 쓰기·Samsung 실기기 동작은 별도 검증이 필요합니다. 새 Rules는 구버전의 Consult 직접 쓰기를 차단하므로 **앱을 v167로 업데이트해야 합니다.**

## 정리·복구

- 미사용 구형 Consult v159, Site Work v146 JS/CSS 및 구형 시드/템플릿/중복 아이콘, 제거된 모바일 Paper CSS/시작 동의 JS를 정리했습니다.
- Git에 올라가지 않은 미완성 v166 사이트 ZIP 2개도 현재 폴더에서 제외했습니다.
- 이전 파일은 `C:/AiderLogBuild/retired-assets-v166`, `C:/AiderLogBuild/retired-assets-v167` 및 기존 Git 이력으로 복구할 수 있습니다. 실제 사용자 기록이나 Git 이력을 삭제하지 않았습니다.
- 교체된 v165 APK/ZIP 3개는 별도 복구 폴더에 검증된 사본을 보관하고 배포 폴더에서 제거했습니다. 여전히 참조하는 이전 버전명 소스 파일은 보존합니다.
- 개별 Git 파일 최대값은 새 APK 63,549,161바이트로 100MB 미만입니다. Git 이력은 재작성하지 않았으므로 과거 이력 전체 용량까지 줄인 것은 아닙니다.

## 다음 적용 단계

Work를 처음 사용할 때 계정 소유자가 공간 생성을 확인하고 실제 사용할 친구만 등록·초대합니다. PC ZIP은 브라우저 소스와 HTTPS 시작 바로가기이며 독립적인 Windows EXE나 오프라인 Work 서버가 아닙니다. v13 위젯과 별도 앱 Consult/Work 리디자인은 다음 작업 범위입니다.
