# AiderLog v169 / Android 1.9.59

## 변경 범위

- Android: 기존 위젯 ID와 개별 설정을 유지하고 갱신 진입점을 통합했습니다. 런처의 실제 폭·높이와 Android 12 이상의 크기 목록을 반영하며, 목록용 데이터와 선택 상태를 크기별로 분리합니다. 새 렌더 실패 시 과거의 텍스트 전용 디자인으로 조용히 돌아가지 않고 짧은 재연결 상태와 진단 로그를 남깁니다.
- 위젯: 기본 라벤더 배경·짙은 글자, 역할별 글자 크기, 조밀한 여백, 30개 설치 목록 이미지 및 native previewLayout을 갱신했습니다. 설치 목록 예시는 실데이터에 저장되지 않습니다. 설정은 기존 런처의 길게 누르기 → 설정 흐름을 유지합니다.
- 앱 색상: Android의 실제 시스템 라이트/다크 상태를 전달하고 WebView 강제 다크 변환을 차단했습니다. 기존 선택 색상을 초기 실행 때 System으로 덮어쓰지 않습니다.
- PC 사이트: 세부 화면의 선택·비활성 상태를 명시적으로 처리하고 오래된 캐시와 새 HTML이 섞이지 않도록 UI 리소스 로딩을 조정했습니다. Work 기준의 본문/버튼/보조 정보/제목 글자 계층을 다른 사이트 화면에도 적용했습니다. 앱의 글꼴·배치에는 사이트 규칙을 넣지 않습니다.

## 검증 범위와 한계

- 격리된 데이터·트랜잭션·네이티브 소스 계약·테마·사이트 화면 전환 회귀 테스트를 수행했습니다. 상세 결과는 별도 검증 ZIP에 있습니다.
- PC 브라우저에서 캘린더/감정 인사이트 반복 이동과 Personal 대시보드 전환을 확인했습니다. 이 검사는 읽기 전용 로컬 fixture이며 운영 기록을 만들거나 수정하지 않았습니다.
- 좁은/넓은 위젯 59개 미리보기는 실제 native XML을 측정하는 브라우저 fixture에서 만들었습니다. 이 PNG는 런처의 설치 목록 대체 이미지이며, **Android RemoteViews 또는 Galaxy One UI에서 촬영한 실기기 스크린샷이 아닙니다.** 실제 설치 위젯의 텍스트·목록·버튼은 네이티브 요소입니다.
- Galaxy가 연결되어 있지 않아 One UI의 실제 추가·스크롤·리사이즈·설정·탭 동작은 미검증입니다. 사용자 사진에 보였던 초기 텍스트 전용 위젯의 정확한 기기 예외는 재현하지 못했습니다. 새 갱신 경로와 오류 진단을 적용했지만 해당 기기 문제가 완전히 해결됐다고 단정하지 않습니다.
- 런처가 보존하지 않는 컬렉션의 픽셀 단위 스크롤 위치까지 보장하지 않습니다. 사용자/위젯/항목/날짜의 식별자와 설정은 유지합니다.

## 데이터 및 배포

이 릴리스는 사용자 데이터 삭제, Firebase 규칙 변경, 유료 서비스 업그레이드, 계정 이전을 하지 않습니다. 우편의 기존 7일 정책과 다른 기록의 무손실 보관 정책도 변경하지 않습니다. v168 APK·사용자 ZIP은 새 파일 검증 후 배포 트리에서 제외하되 로컬 복구본과 Git 이력을 유지합니다. Git 이력을 강제 재작성하지 않습니다.

Windows 다운로드는 두 디자인의 사이트 소스와 HTTPS 시작 링크입니다. 별도의 EXE나 오프라인 백엔드가 아니며, 로그인과 Work/Consult 쓰기에는 운영 사이트 연결이 필요합니다.
