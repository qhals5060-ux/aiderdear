# 직원 개인 공간 v167

## 진입·분리

- 진입점: `employee.html`. 최초 초대: `employee.html?invite=<일회용 토큰>`.
- Google 로그인 후 **초대 연결**을 눌러 서버에서 본인 이메일·초대·소속을 확인한다. 성공하면 주소에서 초대 토큰을 제거한다. 이후에는 같은 `employee.html` 주소로 접속한다.
- `employee-auth-v167.js`는 기존 Firebase 프로젝트의 공개 설정과 Google 인증만 사용한다. 별도 named Firebase app과 session persistence를 사용하며, 메인 `firebase-app.js`, 프로필, 공유 데이터, 대표 index는 불러오지 않는다.
- 최초 `context`가 직원 권한을 확인하기 전에는 개인 메뉴·기록을 렌더하지 않는다. 대표 계정에는 직원 전용 페이지라는 상태만 표시하며 역할 전환은 없다.
- API 클라이언트는 생성 시 UID/화면 세대를 캡처한다. 토큰 요청 전후의 동일 UID 검사로 계정 변경 뒤 이전 payload가 새 UID로 재시도되지 않게 한다. 개인 데이터는 메모리에만 보관하고 로그아웃/계정 변경/권한 거부 시 즉시 비운다.

## 화면·저장 계약

- 나의 홈 / 내 업무 / 업무 기록 / 실험노트 / 누적 기록의 다섯 메뉴.
- 본인 업무의 요청·완료 조건·허용 자료에서 진행 시작/진행 불가와 결과 작성으로 연결한다. 담당 UID나 프로젝트 권한은 수정할 수 없다.
- 업무 기록과 실험노트는 `draftSave`로 서버 개인 저장소에 저장한다. 자동 저장과 수동 임시 저장은 제출이 아니다.
- 초안 폼은 기본 정보, 방법, 결과·QC, 자료, 개인 메모로 구분한다. QC는 항목/시료/값/단위/기준/판정/근거를 별도로 입력하고 서버가 기준 없는 판정을 `미판정`으로 정규화하면 화면에도 그 값을 반영한다.
- 제출은 `preview`가 반환한 실제 allowlist payload를 읽고 `previewHash`와 `expectedRevision`으로 확정한다. 개인 메모는 미리보기와 제출본에 포함하지 않는다.
- v1 제출 후 개인 원문을 수정하거나 첨부를 제외해도 v1은 변경하지 않는다. 보완본은 새 버전으로 제출한다. 대표 검토와 공개된 검토 이력만 읽으며, 직원은 업무를 대표 대신 완료 처리하지 않는다.
- 새로운 복제는 새 ID·현재 작성 일시·`copiedFrom`을 사용하고 기존 제출 버전 정보를 초기화한다.
- 네트워크 결과가 불명확하면 동일 requestId로 확인·재시도한다. revision 충돌은 자동 덮어쓰기하지 않는다. 입력을 유지하고 **서버본 다시 열기**를 사용자의 확인 후 제공한다.
- 모든 누적 페이지를 읽고 검색·기간·프로젝트·유형·상태 필터와 본인 범위 JSON 내보내기를 제공한다. 검색 blur로 버튼을 다시 렌더하여 첫 클릭이 유실되는 문제도 방지했다.
- 새로고침, 화면 복귀, 보이는 페이지에서 60초 간격의 권한/변경 확인을 사용한다. 서버 권한이 거부되면 열려 있는 개인 폼·미리보기도 닫는다. 이미 받은 내용을 영원히 회수할 수 있다고 주장하지 않는다.

## 첨부

- 현재 서버에서 검사하는 형식: JPEG, PNG, WebP, PDF, MP4, WebM, 일반 텍스트, CSV. 파일당 최대 25MiB, 기록당 최대 20개.
- `work-client-v167.js`의 조각 업로드/보호 다운로드를 사용하며 파일 크기와 SHA-256을 확인한다. 공개 파일 URL을 만들지 않는다.
- 실패한 첨부는 재시도/선택 취소가 가능하고 업로드가 끝나지 않은 채 제출을 완료하지 않는다. 제출 미리보기 안에서 첨부 열기는 보호된 실제 다운로드로 제공하여 중첩 모달을 만들지 않는다.
- 개인 첨부와 제출 첨부의 복제·소유권·권한 검사는 서버 책임이다. 인프라 관리자를 암호학적으로 차단한다고 주장하지 않는다.

## 통합·운영 확인

필요 정적 파일: `employee.html`, `employee-auth-v167.js`, `employee-v167.js`, `employee-v167.css`, `work-client-v167.js`.

서버: `/api/work`와 `server/firebase-admin.mjs`, Work 모델, 실제 Firebase Rules가 함께 필요하다. 공개 Firebase 웹 설정은 비밀 키가 아니다. 서비스 계정 비밀값은 서버 환경에만 두며 이 문서에 넣지 않는다. Google 인증 도메인, 서버 환경변수, Rules 배포, 운영 API 배포는 별도 릴리스 절차로 확인한다.

메인 사이트에서 직원 UID의 선행 차단, 서비스 워커의 직원 전용 fallback, 민감 API·초대·미디어 no-store 정책은 통합 담당 범위다. `employee.html`의 오프라인 fallback을 대표 `index.html`로 만들면 안 된다. 직원 포털은 오프라인 개인 데이터 복구를 제공한다고 표시하지 않는다. 페이지를 닫지 않은 오프라인 입력은 메모리에 남지만 서버 저장에 실패한 입력은 페이지를 닫으면 복구할 수 없으며, 이때 닫기 경고를 표시한다.

## 검증

격리된 실제 브라우저 테스트: `tools/employee-ui-test-v167.cjs`.

- 1440×900 / 1280×720 / 390×844 / 360×844에서 다섯 메뉴, 긴 폼, 미리보기, 푸터·모서리·가로 잘림 확인.
- 실제 공용 Work 클라이언트와 실제 순수 서버 검증 모델을 연결한 API fixture 사용. 205개 기록 전체 읽기/필터 내보내기, 업로드 실패·재시도, 중복 방지, 보완 이력, UID 전환·로그아웃·권한 회수, 늦은 응답과 새 UID 재시도 차단을 통과했다.
- 결과: `C:/AiderLogBuild/qa-v167/employee/results.json`, 같은 폴더 PNG. JavaScript 오류 없음.

실제 Firestore/API UI 테스트: `tools/employee-emulator-ui-v167.mjs`.

- 고정 테스트 프로젝트 `demo-aiderlog-employee-ui167`, 로컬 에뮬레이터 `127.0.0.1:8897`만 사용한다. 이 격리 프로젝트의 테스트 문서만 초기화한다. 운영 프로젝트·계정·기록은 사용하지 않는다.
- 현재 저장소와 SHA-256이 같은 API dispatch를 실제 Firestore Admin SDK에 연결했다. 브라우저→공용 클라이언트→현재 API→실제 에뮬레이터 저장→다시 읽기 흐름을 검증했다.
- 일회용 초대, 본인 배정, 서버 재읽기, 개인 초안의 대표/다른 직원 거부, 보호 첨부 저장·다운로드, allowlist 미리보기, 불변 v1/v2와 과거 첨부, 보완 의견·이력, revision 충돌, 실제 소속 회수까지 9개 흐름이 통과했다. 브라우저 요청 65건, JavaScript 오류 없음.
- 결과: `C:/AiderLogBuild/qa-v167/employee-emulator/report.json`.
- Google OAuth 팝업과 Firebase ID 토큰 발급·검증은 이 테스트에서 명시적으로 대체했다. 실제 운영 OAuth, 배포 도메인, 실기기, 운영 Rules 적용 여부는 미검증이다. Firestore Rules 별도 권한 테스트 결과와 이 UI 테스트 결과를 구분한다.

테스트는 외부 Chromium/Playwright 및 에뮬레이터 의존 경로를 상단에 명시한다. 의존성·스크린샷·에뮬레이터 데이터는 릴리스 정적 파일에 포함하지 않는다.
