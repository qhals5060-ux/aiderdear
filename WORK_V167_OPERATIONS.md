# Work / Consult v167 운영 전환 안내

상태: **2026-09-07 운영 웹/API·APK/PC 다운로드와 최종 Firebase 규칙을 게시했습니다.** 적용 결과는 `ROLLOUT_V167_RESULT.md`를 기준으로 합니다. 실사용 데이터 이전·삭제, 친구 초대 발급과 테스트 기록 생성은 하지 않았습니다.

## 운영 전제

- 기존 Firebase 프로젝트 `aiderdear-1bbca`와 Google 로그인을 유지합니다. 신규 서비스 가입은 필요 없습니다.
- 친구들끼리 사용하는 비상업적 개인 사이트입니다. `직원`은 기능의 역할 이름이며 유료 사업 운영을 뜻하지 않습니다. Firebase Spark와 Vercel Hobby를 그대로 유지했습니다.
- 대표 계정은 `qhals5060@gmail.com`, `aidway55@gmail.com`이며 각 UID의 Work/Consult는 독립입니다. 두 계정의 개인 자료는 자동으로 공유되지 않습니다.
- 직원은 대표가 지정한 이메일로 48시간 유효·1회 사용 초대를 수락해야 합니다. 이후 `/employee.html` 고정 주소를 사용합니다. 직원 수는 고정 슬롯이 아닙니다.
- 직원 페이지에는 다른 AiderLog 메뉴/대표 전환이 없습니다. 서버는 화면 표시와 별개로 소속과 권한을 검사합니다. 직원 계정은 기존 사이트 데이터 접근도 차단됩니다.
- 프로젝트·업무·제출본·첨부 조회는 온라인 인증이 필요합니다. 직원 개인 초안을 localStorage나 공용 Service Worker 응답 캐시에 넣지 않습니다. 통신 실패 시 화면의 편집 내용은 유지되지만, 서버 저장 전에 브라우저를 닫으면 미저장 편집은 복구할 수 없습니다.

## 배포 전에 준비할 값 — 비밀값을 Git 또는 문서에 넣지 마세요

Vercel 프로젝트 `aiderdear_1`의 운영/검증 환경에 다음 이름이 필요합니다.

- `PUBLIC_APP_URL`: 해당 배포에서 요청을 받는 정확한 HTTPS origin. 운영값은 `https://aiderdear1.vercel.app`. 검증 배포는 자신의 origin을 별도로 설정합니다.
- Firebase Admin 인증: 기존 `FIREBASE_SERVICE_ACCOUNT_JSON` 또는 `FIREBASE_PROJECT_ID`, `FIREBASE_CLIENT_EMAIL`, `FIREBASE_PRIVATE_KEY` 조합. 서비스 계정은 해당 프로젝트의 필요한 Firestore/Auth 작업 권한만 부여합니다.
- 기존 Google Calendar 환경변수는 변경하지 않습니다. 이 기능은 기존 Calendar 설정에 임의로 값을 추가하거나 덮어쓰지 않습니다.

저장소의 `server/firebase-admin.mjs`가 기존 설정을 읽습니다. 의존성은 `package.json`의 `firebase-admin`이며 런타임 Node 20 이상입니다. 운영 환경의 기존 Admin 설정 존재와 PUBLIC_APP_URL을 확인했고 실제 로그인 후 Work/Consult 읽기로 인증·DB 연결을 확인했습니다. 비밀값은 열람·복사하거나 변경하지 않았습니다.

## 전환 순서 — 규칙만 먼저 게시하지 마세요

1. 데이터 이전·삭제 전에는 별도 백업이 필요합니다. 이번에는 유료 관리형 전체 백업을 사용하지 않고, 기존 규칙 원본·Git 복구본·이전 설치 파일을 보관했습니다. 이것은 전체 사용자 데이터 백업이 아닙니다. 실사용 데이터 이전·삭제는 수행하지 않았습니다.
2. 격리된 검증 프로젝트에서 새 API, 웹, 앱 동기화 어댑터와 규칙을 함께 검증합니다. 테스트용 Firebase 프로젝트/계정으로 Google 로그인, 이메일 확인, 로그아웃을 직접 검증합니다.
3. 최신 앱에도 `consult-sync-v167.js`와 수정된 `firebase-app.js`가 포함되어야 합니다. **기존 v165 APK/사이트는 Consult를 직접 저장하므로 새 규칙을 먼저 게시하면 그 구버전의 Consult 저장이 차단됩니다.** 일반 일정·메모와 원본 읽기 권한은 유지됩니다.
4. 이번 운영 전환은 임시 호환 규칙 → v167 웹/API와 APK/PC 다운로드 → 실제 인증 읽기 확인 → 최종 규칙 순서로 완료했습니다. 기존 앱은 v167 APK로 업데이트해야 Consult 저장을 계속할 수 있습니다.
5. 수동 규칙 게시 위치: https://console.firebase.google.com/project/aiderdear-1bbca/firestore/databases/-default-/security/rules . 저장소 `firestore.rules` 전체를 사용합니다. 부분 규칙을 덧붙이면 기존의 넓은 allow에 의해 보호가 무효화될 수 있습니다.
6. 대표 본인의 Work에서 공간 생성 확인 → 직원 등록 → 초대 링크 발급/복사 → 직원이 자신의 Google 계정으로 수락합니다. 실사용 직원/업무를 테스트 데이터로 만들지 마세요.
7. 최소 2개 직원 계정으로 자신의 업무/비공개 초안/제출/대표 검토를 확인하고, 다른 직원·다른 대표·미인증·권한 회수 계정의 접근이 거부되는지 확인합니다. 실제 Samsung 기기의 앱 저장/복귀도 별도 확인합니다.

## 저장 경계

- `workspaces/{ownerUid}`: 대표별 프로젝트, 업무, 직원, 지출, 구매, 자료, 제출본, 검토, 미디어, 변경 이력.
- `workIdentities/{employeeUid}`: 서버가 생성한 직원 소속 표식. 본인은 get만 가능하며 role 쓰기는 불가합니다.
- `employeePrivate/{employeeUid}/drafts/{id}`: 해당 직원만 API로 사용하는 초안. 대표도 조회할 수 없습니다.
- 제출본은 개인 메모가 제외된 서버 미리보기의 해시를 확인한 뒤 새 ID/version으로 저장합니다. 제출 후 대표가 원문을 수정할 수 없고 검토 의견/보완 요청은 별도입니다.
- 파일은 25MB 이하, 384KB 조각 업로드입니다. 각 조각 읽기에도 현재 소속을 확인합니다. 공개 다운로드 토큰을 발급하지 않습니다. 이전 제출본/자료 버전의 파일 참조는 보존합니다.
- Consult는 기존 `users/{uid}/private/main`의 4개 배열/원래 ID를 유지합니다. 클라이언트 전체 저장에서 해당 배열 변경을 차단하고 `/api/consult` 트랜잭션으로만 수정합니다. 전달본·문서 버전은 원본 변경을 막고 신규 개인 첨부도 연결 시 실제 바이트와 SHA를 확인하여 잠급니다.
- 기존 공개/공유 저장소의 옛 Consult 첨부는 자동 삭제하거나 복사하지 않습니다. 기존 참조를 보존하며, 신규 첨부부터 계정별 비공개/잠금을 적용합니다.
- Consult의 기존 단일 Firestore 문서가 너무 크면 850KB 안전 한도에서 저장을 거부하고 분할 이전을 안내합니다. 배열을 잘라 저장하지 않습니다. 무제한 저장/자동 분할 완료를 의미하지 않습니다.

## 기존 Work / 실험노트 이전

- 기존 `workRecords`, 연구원 고정 링크와 제출 원본은 자동 삭제하지 않습니다.
- Work의 이전 자료 확인에서 원본과 새 대상 유형을 직접 확인합니다. 담당자는 실제 연결된 UID로 명시 확인하고, 이름 문자열만으로 추정 배정하지 않습니다.
- 이전본에 원본 ID/경로/해시/원본 데이터를 남깁니다. 원본이 바뀌었거나 대상 ID가 이미 있으면 덮어쓰지 않습니다. 담당자 확인 전 기록은 배정 업무로 공개되지 않습니다.
- 기존 링크를 일괄 폐기하지 않습니다. 백업·이전과 직원 안내 후 대표가 명시적으로 닫을 때만 `legacyClose`를 실행합니다. 회수된 직원의 기록도 정책상 보존하되 접근 권한만 제거합니다.
- 실험 결과의 QC 기준이 없으면 `미판정`입니다. 검토 완료가 업무 완료나 비용 지급을 자동 의미하지 않습니다. 대표가 후속 업무/완료를 별도로 확인합니다.

## 검증과 한계

격리 Firestore 에뮬레이터에서 서버 및 실제 Rules를 함께 검사했습니다. 브라우저 UI 테스트에서도 같은 API dispatch와 에뮬레이터를 연결했습니다. 테스트 데이터는 `demo-*` 프로젝트만 사용했습니다. 운영 배포 뒤 기존 로그인 세션 복원과 인증된 Work/Consult 조회도 확인했습니다. 신규 OAuth 동의 흐름, 다른 친구 계정, 실사용 쓰기·초대·이전, 실물 Samsung 기기는 검증하지 않았습니다.

실패 시 현재 입력을 유지하고 재시도하거나 새로고침 전 별도 복사하세요. 충돌은 최신 내용을 확인한 뒤 의도적으로 다시 수정합니다. 자동 재시도는 동일 requestId를 사용하여 중복 제출을 막습니다. 구버전 클라이언트로 보호된 원본을 덮어쓰는 방식으로 복구하지 마세요.
